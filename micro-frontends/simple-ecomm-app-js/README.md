The project is a simple example for micro-frontend using simple JS ( without any frameworks ) and using webpack module federation. 

- Container ( root that loads other pages/apps )
- Products, Cart ( independent items )